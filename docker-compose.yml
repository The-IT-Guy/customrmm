version: "3.9"

services:
  app:
    build: .
    container_name: customrmm-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - customrmm-data:/app/data

volumes:
  customrmm-data:
